# NovaTicket Frontend

React 19 + Vite UI for the NovaTicket platform.

Pages: Home (event browsing), EventDetail (reviews + similar events), Dashboard (user history), Recommendations (ML), Login, Register.

```bash
npm install
npm run dev          # → http://localhost:5173
npm run build        # production build
```
